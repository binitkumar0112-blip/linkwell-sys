{
  "name": "linkwell-monorepo",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "python -m uvicorn main:app --app-dir backend --reload",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint"
  }
}
